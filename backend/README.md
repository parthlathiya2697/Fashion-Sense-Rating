```bash
python -m venv venv
source venv/bin/activate


# Create a Django Project: Create a new Django project and app
django-admin startproject backend
cd backend
python manage.py migrate
python manage.py runserver
```

